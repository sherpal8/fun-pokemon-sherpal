const { Character, grassCharacter, Trainer } = require("../lib/trainer.js");
// const { Battle } = require("../battle.js");   ---> to uncomment when/if needed
const { expect } = require("chai");

describe("", () => {
  it("", () => {});
});
